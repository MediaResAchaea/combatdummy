import React from 'react';
import ReactDOM from 'react-dom';
import EnemyAfflictionTracker from './EnemyAfflictionTracker';

ReactDOM.render(
  <React.StrictMode>
    <EnemyAfflictionTracker />
  </React.StrictMode>,
  document.getElementById('root')
);
